<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_password_reset"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.himanshu.rentbasera.com.example.himanshu.rentbasera.PasswordReset">

    <TextView
        android:id="@+id/resetPasswordTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Reset Your Password"
        android:textSize="25dp"
        android:gravity="center_horizontal"

        />

    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"></View>

    <EditText
        android:id="@+id/otpEditBox"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_below="@+id/resetPasswordTextView"
        android:layout_marginTop="20dp"
        android:hint="Enter OTP"
        android:textSize="20dp"
        android:inputType="number"
        android:background="@drawable/rectangle_border"
        />
    <EditText
        android:id="@+id/NewPasswordEditText"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_below="@+id/otpEditBox"
        android:layout_marginTop="20dp"
        android:hint="New Password"
        android:textSize="20dp"
        android:inputType="textPassword"
        android:background="@drawable/rectangle_border"
        />
    <EditText
        android:id="@+id/ConfirmPasswordEditText"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_below="@+id/NewPasswordEditText"
        android:layout_marginTop="20dp"
        android:hint="Confirm Password"
        android:textSize="20dp"
        android:inputType="textPassword"
        android:background="@drawable/rectangle_border"
        />

    <Button
        android:id="@+id/resetButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/ConfirmPasswordEditText"
        android:layout_marginTop="20dp"
        android:hint="Reset"
        android:textSize="30dp"
        android:background="@drawable/button_style"
        />
</RelativeLayout>
